//! The agent loop. Implemented in Phase 1 — see `docs/plan.md` and
//! `docs/architecture.md` §"Agent loop" for the algorithm this module must implement.
//!
//! It stays deliberately empty until the contracts below it are exercised by real
//! plugins, because a loop written against imagined tool and model behavior tends to
//! encode the wrong seams.
