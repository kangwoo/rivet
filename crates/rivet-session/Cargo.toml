[package]
name = "rivet-session"
description = "Session persistence: append-only event log, replay, fork."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
rivet-core.workspace = true
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio = { workspace = true }
# Torn-tail recovery reports what it repaired. `RuntimeEvent` has no variant for it and
# adding one would be a contract change, so it goes to `tracing`.
tracing.workspace = true

[dev-dependencies]
tempfile.workspace = true
tokio = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
